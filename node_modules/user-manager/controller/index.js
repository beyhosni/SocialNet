var Users = require('../../../models/Users').Users;
var mongoose = require('mongoose');


function UserManager(firstName,lastName,email,password) {
    this.firstName = firstName;
    this.lastName = lastName;
    this.email = email;
    this.password = password;


};

UserManager.prototype.userCreate = function (callback) {

};
UserManager.prototype.userUpdate = function (callback) {

};
UserManager.prototype.userDelete = function(callback){
    
};
